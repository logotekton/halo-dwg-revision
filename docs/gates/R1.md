# R1 게이트 (1주차 MVP)

Seed: `docs/seeds/R1-mvp-2026-09-04.yaml`. 장부: `docs/plans/dms-local/interview-mvp-2026-09-04.md`. 계약: `docs/contracts/r1.md`.

합격 판정 줄은 `R1-GATE <항목>: PASS|FAIL` 형식으로만 적는다. 아래 "필수" 네 줄이 모두 PASS이고 "사용자 승인" 항목이 채워져야 R1이 끝난다. "추가(비필수)" 줄은 Seed의 exit_conditions에는 들어가지 않지만, 각 태스크 보고서가 Windows에서 확인해 달라고 요청한 항목이라 여기 함께 남긴다 — FAIL이어도 R1 자체를 막지 않되, 원인은 "남은 문제"에 반영한다.

## 자동 검사 (R1-11, worktree에서 재확인)

R1-11은 `/Users/ythong/Desktop/대명건설/.worktrees/R1-11`(브랜치 `task/R1-11`)에서 실행했다 — main과 같은 커밋이다.

```
$ git log -1
commit 90160f9449d84712a3c2981e336995fe8bfb73d8
Date:   2026-09-04 20:39:30 +0900
docs: gate R1 AC4 result (vertical-slice e2e green on main 7c9bda8)
```

| 항목 | 명령 | 결과 |
|---|---|---|
| AC1 선행 병합 | `test -f packages/cad-core/src/host/cad-host.ts && grep -rqi visib packages/cad-core/src/host && test -f engine/src/halo_engine/ingest/xref.py` | OK (2026-09-04, worktree R1-11 @ 90160f9, main과 동일 커밋) |
| AC2 Windows 설치본 CI 정의 | `grep -q windows-latest .github/workflows/windows-installer.yml && grep -qi nsis apps/desktop/electron-builder.yml && grep -q comtypes engine/pyproject.toml` | OK (2026-09-04, worktree R1-11 @ 90160f9) |
| AC3 엔진 비교 테스트 | `cd engine && uv run --frozen pytest tests/compare -q` | **405 passed, 2 skipped** (2026-09-04, worktree R1-11 @ 90160f9, 16.25s). `RC=0` |
| AC4 verify + e2e | `perl -e 'alarm 1500; exec @ARGV' -- tools/verify.sh --e2e` | **VERIFY: PASS**, `RC=0` (2026-09-04, worktree R1-11 @ 90160f9). e2e 16 passed / 7 skipped(은퇴 셸 1, 실도면 6); 엔진 737 passed / 2 skipped, 165.92s. `tests/e2e/compare.spec.ts`가 세트 지정 → 도곽 목록 → 검토(승인·무시) → 전체 도곽 출력을 한 번에 통과 |

AC1·AC2·AC3·AC4 모두 main(78b00cb/7c9bda8)에서 기록한 이전 결과와 같은 방향으로 일치한다(AC4의 e2e·엔진 통과 건수가 정확히 같다: 16/7, 737/2). AC3의 세부 건수(405 vs 이전 기록 343)는 실행 범위 차이가 아니라 그 사이 병합된 태스크(R1-07·R1-08·R1-09·R1-10)가 `tests/compare`에 테스트를 계속 추가했기 때문이다 — main의 최신 기록이 이 값을 갱신하지 않았을 뿐, 실패는 없다.

**worktree 준비 메모(재현 시 필요):** 새로 만든 worktree는 `node_modules`가 없다. `cd engine && uv run --frozen pytest tests/compare -q`를 그냥 돌리면 `test_zwcad_failure_falls_back_to_builtin_and_forces_same_converter`가 `builtin`(acad-ts) 경로에서 `Cannot find package '@node-projects/acad-ts'`(또는 `@halo-cad/schema/dist`)로 FAIL한다 — 코드 결함이 아니라 `pnpm install --frozen-lockfile`과 `pnpm --filter @halo-cad/schema build`를 먼저 하지 않아서다(둘 다 `tools/verify.sh`가 내부적으로 하는 일이라 `tools/verify.sh --e2e`만 돌리면 이 문제가 없다). 이 태스크는 두 명령을 먼저 실행해 위 405 passed를 얻었다. `docs/dev/ci.md`의 "로컬 재현" 절이 `tools/verify.sh`만 언급하고 `tests/compare`만 단독 실행하는 경로는 언급하지 않아, "Shared-file patch"에 문서 보강을 제안했다.

## 사용자 확인 (Windows 노트북, 설치본으로만)

설치본(첫 빌드, main@40375d8, 2026-09-04): https://github.com/logotekton/halo-dwg-revision/actions/runs/33849250597 → Artifacts `halo-dwg-revision-windows-x64`(177 MB zip, 2026-09-18 만료). 잡 4분 20초. D0·D1 범위(셸·엔진 인입 API).
설치본(D2 포함, main@6e4625d, 2026-09-04): https://github.com/logotekton/halo-dwg-revision/actions/runs/33856626045 — 화면 A(세트 지정)·B(도곽 목록), 인입·도곽 추출·매칭 API 포함. CI 필수 잡 전부 녹색. 이 빌드로 `installer`·`sheet-count` 항목을 미리 확인할 수 있다(화면 C·D는 아직 자리표시자).

**설치본(최종, main@237f203, 2026-09-04): https://github.com/logotekton/halo-dwg-revision/actions/runs/33870505763 → Artifacts `halo-dwg-revision-windows-x64`(zip, 14일 보존; 만료되면 Actions 탭에서 "Windows Installer" 워크플로를 Run workflow로 다시 만든다).** R1-00a~R1-11·R1-06b가 전부 들어간 빌드다. 참고: main에 R1-10·R1-11이 모두 들어간 뒤 GitHub Actions "Windows Installer" 워크플로(`.github/workflows/windows-installer.yml`)의 **가장 최근 `main` 실행**을 https://github.com/logotekton/halo-dwg-revision/actions 에서 찾아 그 실행의 Artifacts `halo-dwg-revision-windows-x64`(보존 14일) 링크를 여기 적는다. 아래 절차는 전부 이 최종 설치본으로 한다 — 위 두 링크(D0·D2 범위)는 화면 C·D가 없어 `owner-pair`·`markup-dwg-opens`를 확인할 수 없다.

### 사용자 PC 경로

- 실도면 68장(`samples/2026-09-02-실시도서/`을 옮긴 경로): (기록 필요)
- 리비전 쌍(`samples/revision-pairs/<이름>/`): (기록 필요 — Q5, 아직 안 왔으면 "쌍 제공 후 수행"으로 두고 owner-pair는 대기)
- ZWCAD: 2026 Professional, 설치 경로: (기록 필요), 라이선스 상태: (기록 필요)

### 절차 (단계별)

준비물: 위 "설치본(최종)" 링크, 실도면 68장 폴더(공종별 6개 하위 폴더), 리비전 쌍 폴더(`samples/revision-pairs/<이름>/{before,after}`, `planted.md` — 형식은 `samples/revision-pairs/README.md`).

| # | 단계 | 사용자 행동 | 확인할 것 | 스크린샷 |
|---|---|---|---|---|
| 1 | 설치 | zip을 풀고 `Halo CAD Setup <version>.exe` 실행 → SmartScreen "추가 정보"→"실행" → 한국어 마법사에서 설치 경로 확인 후 "설치" | 설치가 오류 없이 끝나고 앱이 실행된다 | `docs/gates/img/R1-installer-01.png` |
| 2 | 실행 | 앱 실행 후 상태바를 본다 | 상태바가 "엔진: 연결됨 v0.0.1"이고 오류 토스트가 없다(`status.engine.ready`) | `docs/gates/img/R1-installer-02.png` |
| 3 | 실도면 세트 지정(공종별) | 화면 A에서 "폴더 선택…"을 전·후 각각 눌러 **같은 공종 폴더를 전=후에 똑같이** 지정(공종 폴더 6개를 이 표 순서대로 한 번씩) → 실행 날짜는 오늘 그대로 → "인입 시작" | 요약 카드에 "파일 N개"·"실패 0"(또는 실패 사유가 아래 3장뿐) | `docs/gates/img/R1-sheet-count-<공종>-01.png` |
| 4 | 화면 B 도곽 수 읽기 | 인입이 끝나면 자동으로 화면 B(도곽 목록)로 넘어간다 — 표 행 수(또는 상태 필터 "전체"의 총 개수)를 센다 | 아래 "도곽 수" 표에 실측값을 채운다 | `docs/gates/img/R1-sheet-count-<공종>-02.png` |
| 5 | 리비전 쌍 지정 | 화면 A로 돌아가(또는 앱 재실행) 전=`samples/revision-pairs/<이름>/before`, 후=`.../after`로 "폴더 선택…" → "인입 시작" | 요약 카드 "실패 0" | `docs/gates/img/R1-owner-pair-01.png` |
| 6 | 비교 실행 | 화면 B에서 "비교 실행" | 상태 칩이 "변경"/"동일"로 바뀐다 | — |
| 7 | 화면 C 승인·무시 | 변경된 도곽마다 "도곽 열기" → 클러스터 목록을 `planted.md`와 대조 → 심은 자리는 "승인", 심지 않은 자리(있다면)는 "무시" | 클러스터 수·자리가 `planted.md`와 맞는지 아래 "클러스터" 표에 채운다 | `docs/gates/img/R1-owner-pair-02.png` |
| 8 | 화면 D 출력 | 도곽 목록으로 나와 "전체 도곽 출력…" → 실행 날짜 확인 → "출력 실행" | 결과 표에 도면번호별 파일·형식·라이터가 뜨고, "출력된 도면이 없습니다"가 아니다(승인이 하나라도 있다면) | `docs/gates/img/R1-owner-pair-03.png` |
| 9 | 출력 폴더 확인 | "폴더 열기" | `<프로젝트>/출력/<날짜>/`가 열리고 `.dwg`(또는 ZWCAD 없으면 `.dxf`) + `changes.tsv` + `run.json`이 있다 | — |
| 10 | ZWCAD 2026에서 열기 | 출력된 도면 파일을 ZWCAD 2026에서 연다 | 레이어 `REV-<YYYYMMDD>`(빨강), 승인한 자리마다 구름+삼각형+번호, 무시한 자리는 아무것도 없음, 표제란 왼쪽에 리비전 표(번호·내용·일자), `REV-*` 레이어를 끄면 원본 그대로 | `docs/gates/img/R1-markup-dwg-opens-01.png` |

### 도곽 수 (sheet-count)

| 공종 | 상수(장부) | 실측(Windows, 설치본) | 일치 | 비고 |
|---|---:|---:|---|---|
| 전기 (`03_전기`) | 104 | (기록 필요) | | mac 변환 경로에서 1장이 안 읽힘(Q9) — Windows(ZWCAD 변환)에서 읽히는지 특히 확인 |
| 기계 (`02_기계`) | 51 | (기록 필요) | | |
| 통신 (`04_통신`) | 28 | (기록 필요) | | |
| 소방기계 (`05_소방_기계`) | 45 | (기록 필요) | | mac 변환 경로에서 1장(내진도면)이 안 읽힘(Q9) |
| 소방전기 (`06_소방_전기`) | 28 | (기록 필요) | | |
| 건축 (`01_건축`) | 99 | (기록 필요) | | mac 변환 경로에서 1장(`A-060`)이 안 읽힘(Q9) — 이 3장이 Windows에서도 안 읽히면 `sheet-count`는 FAIL, 안내 문구("도곽 미인식"/실패 파일 목록)로 사유가 남는지도 함께 본다 |

### 클러스터 (owner-pair, 리비전 쌍마다 한 표)

리비전 쌍 폴더 이름: (기록 필요). `planted.md` 요약:

| 도면번호 | 위치/부근 | 심은 변경 종류 | 기대 클러스터 수 | 실제 클러스터 수 | 승인/무시 | 비고 |
|---|---|---|---:|---:|---|---|
| (기록 필요) | | | | | | |

기대 = 실제이고 심지 않은 자리에 클러스터가 없어야 `owner-pair: PASS`다. 접힌 항목(이동 0.01mm 이하, 레이어/색/선종류/선굵기만, 해치·치수 재생성, MTEXT 서식만)은 클러스터로 세지 않는다 — 접혔는데 실제로는 유의미한 변경이면 화면 C의 "접힌 항목 n건" 목록에서 찾아 기록한다.

### 출력 파일 경로

- 마크업 DWG/DXF: (기록 필요, 예: `<프로젝트>/출력/2026-09-04/A-101_after_markup.dwg`)
- `changes.tsv`: (기록 필요)
- `run.json`: (기록 필요)

### 참고: 실도면 도곽 수 mac 측정값 (acad-ts 변환 기준, `docs/dev/compare-frames.md`)

mac 개발 환경은 ZWCAD가 없어 acad-ts로 변환한 작업용 DXF로만 실측했다. acad-ts는 ATTRIB을 잃는 경우가 많아 "기본"(ATTRIB 태그) 경로 대신 `block_name_patterns: ["*TITLE BLOCK*"]`("블록명" 경로)로 다시 잰 값이 장부 상수와 대부분 일치한다. **합격 판정은 이 값이 아니라 위 "도곽 수(sheet-count)" 표의 Windows 실측값으로 한다** — 아래는 참고용 대조표다.

| 공종 | 읽힌 파일(acad-ts) | 블록명 경로 도곽 수 | 장부 상수 | 차이 |
|---|---:|---:|---:|---:|
| 전기 (`03_전기`) | 0 / 1 | 0 | 104 | -104 (파일 자체가 안 읽힘, MULTILEADER 결함 — Q9) |
| 기계 (`02_기계`) | 1 / 1 | 51 | 51 | 0 |
| 통신 (`04_통신`) | 1 / 1 | 28 | 28 | 0 |
| 소방기계 (`05_소방_기계`) | 1 / 2 | 16 | 45 | -29 (1장 안 읽힘, MULTILEADER 결함 — Q9) |
| 소방전기 (`06_소방_전기`) | 1 / 1 | 28 | 28 | 0 |
| 건축 (`01_건축`) | 36 / 37 | 94 | 99 | -5 (1장 안 읽힘, MULTILEADER 결함 — Q9. 읽힌 36장 기준으로도 94 vs 99이므로 나머지 5장 차이는 아직 원인 미상 — Windows 실측으로 재확인) |

## 필수 R1-GATE (Seed exit_conditions, 네 줄 모두 PASS 필요)

R1-GATE sheet-count: (대기)
R1-GATE owner-pair: (대기)
R1-GATE markup-dwg-opens: (대기)
R1-GATE installer: (대기)

## 추가(비필수) R1-GATE — 각 태스크 보고서가 요청한 Windows 확인

이 줄들은 Seed의 exit_conditions에 들어가지 않는다. R1을 막지는 않지만, 결과를 "남은 문제"에 반영한다.

| 항목 | 무엇을 확인하는지 | 출처 |
|---|---|---|
| `zwcad-manual-check` | `halo-engine.exe zwcad-convert`로 실도면 5장 변환, 숨은 인스턴스, SaveAs 버전 상수(Q6), 시간 초과·재시작 — 절차는 `docs/dev/zwcad-bridge.md` "Windows 수동 확인 절차" | R1-02 |
| `보기모드-블록` | 화면 C 보기 모드("전"/"후")가 INSERT(블록) 변경에서는 그림을 바꾸지 못하는 알려진 제한을 실도면에서도 확인(선·폴리라인·해치는 정상 동작해야 함) | R1-08 |
| `compare-determinism-win` | 같은 세트를 두 번 비교해 `compare.dxf`·`clusters.json`이 바이트 동일(`Get-FileHash`)한지, 실도면 375도곽 `compare.run` 소요 시간 | R1-06 |
| `번들-마이그레이션` | 기존(이전 빌드로 만든) `.halo` 번들을 새 설치본으로 열 때 마이그레이션 0003이 적용되고 `compare.yaml`·`frames.yaml`이 한국어 주석 그대로 생기는지 | R1-01 |
| `화면C-검토` | ZWCAD 변환 실도면에서 검토 화면이 열리고 클라우드·번호가 보이는지, 한글 표제란 문구, 300장 규모 연속 열기 메모리·속도 | R1-08/R1-06b |
| `출력-tsv-excel` | `changes.tsv`를 Excel에서 열어 한글이 깨지지 않는지(BOM 없는 UTF-8 — 깨지면 `docs/gates/R1-questions.md` Q10 재검토) | R1-09 |
| `원본-불변-win` | 세트 지정·비교·출력을 다 거친 뒤 전·후 원본 폴더의 파일 수정 시각이 그대로인지 | R1-09 |

```
R1-GATE zwcad-manual-check: (대기)
R1-GATE 보기모드-블록: (대기)
R1-GATE compare-determinism-win: (대기)
R1-GATE 번들-마이그레이션: (대기)
R1-GATE 화면C-검토: (대기)
R1-GATE 출력-tsv-excel: (대기)
R1-GATE 원본-불변-win: (대기)
```

## 남은 문제 (우선순위: 게이트 차단 / 2주차 / 개선)

각 태스크 보고서의 Follow-ups·Questions와 `docs/gates/R1-questions.md`를 합쳤다. 기존 표의 행은 하나도 빼지 않았다 — "게이트 확인"이었던 행은 위 필수·추가 R1-GATE 절차로 실제로 확인하게 되므로 **게이트 차단**으로 옮겼다(Windows에서 확인하기 전까지는 해당 R1-GATE 줄을 정직하게 PASS라고 적을 수 없다는 뜻이다).

### 게이트 차단

| 출처 | 내용 |
|---|---|
| R1-04 | 실도면 3장(`03_전기` 도서, `05_소방_기계` 내진도면, `01_건축 A-060`)이 mac의 acad-ts 변환 경로에서 MULTILEADER `90 / None` 결함으로 못 읽힘. Windows(ZWCAD 변환)에서는 안 날 것으로 예상 — 게이트에서 이 3장의 도곽 수를 확인(Q9, 위 "도곽 수" 표) |
| R1-04 | 기본 `frames.yaml`(ATTRIB 태그)로 실도면 도곽 수가 나오는지 Windows에서 확인. mac에서는 acad-ts가 ATTRIB을 잃어 `block_name_patterns: ["*TITLE BLOCK*"]` 경로로만 측정(기계 51·통신 28·소방전기 28 일치) |
| R1-02/R1-03 | 실제 ZWCAD 자동화(숨은 인스턴스, DWG→DXF, 시간 초과·재시작, 같은-변환기 재변환)는 가짜 COM으로만 검증됨(Q6) — 위 `zwcad-manual-check` |
| R1-00a | Windows 설치본에서 뷰어 WebGL 렌더와 숨김 창 DWG 변환(폴백 경로) 확인 |
| R1-06 | ZWCAD 변환 DXF에서의 비교 잡음(안 바꾼 시트가 `same`인지, `hatch_regen`/`dim_regen` 접힘이 비정상적으로 많지 않은지), Windows 결정론(같은 세트 두 번 비교 → `compare.dxf`·`clusters.json` 해시 동일, `Get-FileHash`), 실도면 375도곽 `compare.run` 소요(잡 `extra.elapsed_s`) — 위 `compare-determinism-win` |
| R1-08/R1-06b | 화면 C: ZWCAD 변환 실도면에서 검토 화면이 열리고 클라우드·번호가 보이는지, 한글 표제란 문구, 300장 규모 연속 열기 메모리·속도. 보기 모드: "전"에서 신설·이동한 문·창·치수가 사라지고 "겹쳐 보기"에서 후=빨강·전=시안인지(R1-06b 복제 블록) — 위 `화면C-검토`·`보기모드-블록` |
| R1-09 | 출력 DWG가 `.dwg`로 나오는지(`.dxf`면 ZWCAD 감지 실패), ZWCAD 2026에서 `REV-<날짜>` 레이어·클라우드·삼각 번호·표제란 왼쪽 리비전 표·`REV-*` 끄면 원본 그대로, 같은 날 재출력 `-2` 폴더/레이어, `changes.tsv`가 Excel에서 한글이 깨지는지(BOM 없음 정책 재검토), 원본 폴더 수정 시각 불변 — 위 `markup-dwg-opens`·`출력-tsv-excel`·`원본-불변-win` |
| R1-01 | 새 설치본이 기존 `.halo` 번들을 열 때 마이그레이션 0003이 적용되고 `compare.yaml`·`frames.yaml`이 한국어 주석 그대로 생기는지 — 위 `번들-마이그레이션` |

### 2주차

| 출처 | 내용 |
|---|---|
| R1-03 | `ingest/working_dxf.py`가 캐시 키를 입력 sha로 잡아 DWG 입력 시 원본 sha 이름으로 정본을 한 벌 더 복사(디스크 2배). `cache_key` 인자 추가 제안 |
| R1-03/R1-04/R1-05 | `halo_schema`(packages/schema/gen/python)를 엔진 의존성으로, `@halo-cad/schema`를 apps/web 의존성으로 배선하면 손수 미러한 응답 모델·TS 타입을 걷어낼 수 있음 |
| R1-04 | `sheet-frame` 스키마에 `file_name`(선택) 추가, `repos.create_pair` 추가, `boundary_source` 저장 |
| R1-04 | `03_전기`(20.8 MB, 348k 엔티티)의 acad-ts 변환이 병렬 실행 중 실패(단독 343초 성공) |
| R1-05 | WS `job.progress` 클라이언트(지금은 500ms 폴링, 엔진 원문 message 표시). DXF 입력일 때 요약 카드 "변환 성공 0" 문구 다듬기 |
| R1-00a | 뷰어 힙 20회 왕복 +8.6%, mlightcad 단일 청크 3.56 MB, `apps/web`이 `@halo-cad/dwg-io-gpl`에 의존(convert.html 청크에만), 폰트 없어 TEXT가 비어 보임(W3-05 범위) |
| R1-08 | `packages/cad-core`에 `CadHost.attach(container)`(지금은 화면 C 재진입 시 호스트 재생성), 여는 직후 카메라가 클러스터 근처로 맞춰지는 현상(`zoomToFit` 시점), 접힌 항목의 엔티티 단위 표시·캔버스 강조, 나란히 보기·색 설정 |
| R1-09 | TSV BOM 설정(`output.tsv_bom`, Q10), acad-ts 라이터 실측 없음(명시 설정 시만), 68장 × ZWCAD 저장 성능 실측, `run.schema.json` `output_file.path` 설명 문구(상대 경로) 정정(코드젠 동반) |
| R1-06b | 복제 뒤 참조 없는 원본 `*D` 블록 purge, `test_zwcad_failure_falls_back...`에 acad-bridge 미빌드 시 skip 가드(위 "자동 검사"의 worktree 준비 메모 참고 — 미빌드가 아니라 미설치일 때도 지금은 FAIL이지 SKIP이 아니다) |

### 개선

| 출처 | 내용 |
|---|---|
| R1-05 | 옛 셸 e2e `tests/e2e/shell.spec.ts`를 `test.skip`으로 은퇴(승인). 삭제 여부는 2주차 정리 때 |
| Fable | `test_engine_crosscheck`는 Windows에서 skip(설치 폰트에 따른 TEXT 범위 차이). Windows verify 레그는 비필수 유지 |
| R1-11 | `docs/dev/ci.md` "로컬 재현"에 `cd engine && uv run pytest tests/compare`를 `tools/verify.sh` 밖에서 단독 실행할 때는 `pnpm install --frozen-lockfile && pnpm --filter @halo-cad/schema build`가 선행돼야 한다는 문장을 추가하면 좋겠다(Shared-file patch 참고) |

## 사용자 승인

- 날짜: (대기)
- 승인 문구: (대기)
