# R1-10 화면 D(출력)와 수직 슬라이스 E2E

- Owner model: Sonnet
- Size: 0.5–1 d
- Branch: `task/R1-10` (worktree `/Users/ythong/Desktop/대명건설/.worktrees/R1-10`)
- Wave: R1  Phase: D5 (R1-08·R1-09 병합 후 시작)

## Context
화면 D는 범위(전체 도곽, 고정)·넣는 방법(ZWCAD 있으면 DWG, 없으면 DXF — 엔진이 정한다)·실행 날짜를 보여 주고 출력 잡을 돌린 뒤 결과 파일 목록과 "폴더 열기"·"TSV 복사"를 제공한다. 그리고 Seed AC4의 Playwright 수직 슬라이스(세트 지정 → 도곽 목록 → 검토(승인·무시) → 전체 도곽 출력)를 한 스펙으로 잇는다. 먼저 읽을 것: `CLAUDE.md`, `docs/contracts/r1.md` §7(export)·§8·§9·§10, `docs/dev/compare-ui.md`, `docs/dev/compare-export.md`(R1-09), `apps/web/src/features/compare/**`·`state/{compare,review}.ts`, `test-hooks.ts`, `tests/e2e/{compare-sheets,compare-review}.spec.ts`, `packages/schema/gen/ts/compare/run.d.ts`, `docs/plans/dms-local/01-보고용-계획서.html` §3 화면 D.

## Goal
1. `features/compare/ExportScreen.tsx`(자리표시자 교체): 요약(승인 n건 / 무시 n건 / 대상 도곽 n장), 범위 "전체 도곽"(고정 표시), 넣는 방법 표시(ZWCAD 상태에 따라 "DWG(ZWCAD)" 또는 "DXF(ZWCAD 없음)"), 실행 날짜(세트의 `run_date`, 편집 가능), "출력 실행" → `POST /compare/sets/{id}/export` → 잡 진행 → 결과 표(도면번호·파일·형식·라이터·경고) + 출력 폴더 경로 + 버튼 "폴더 열기"(`shell.openPath`)·"변경 리스트 TSV 복사"(`GET .../tsv` → `clipboard.writeText`) · "목록으로".
2. `state/export.ts`·`features/compare/exportApi.ts`(`startExport`, `getRun`, `getRunTsv`).
3. 테스트 훅: `compareRunExport({runDate})`, `compareGetLastRun()`.
4. `tests/e2e/compare.spec.ts`(수직 슬라이스, Seed AC4 artifact): `HALO_E2E_PICK_FOLDERS`로 `fixtures/compare/S13_multi_sheet/{before,after}` → 화면 A에서 폴더 두 개 선택·날짜 `2026-09-04`·인입 → 화면 B 행 2(A-101 동일·A-102 변경) → 비교 실행 → `A-102` 열기 → 클러스터 승인 1·무시 1 → 화면 D → 출력 실행 → `compareGetLastRun()`의 `files`에 A-102 1건, `output_dir`가 `출력/2026-09-04`로 끝남 → 파일 시스템에서 마크업 파일이 존재하고 텍스트에 `REV-20260904`가 있음(DXF-only 경로; mac에는 ZWCAD 없음) → `changes.tsv`에 승인·무시 2행 → 원본 폴더 해시 불변 → "폴더 열기" 클릭 시 `HALO_E2E_OPENED_PATHS` 기록. 스크린샷 4장(A·B·C·D).
5. i18n `compare.export.*`. `docs/dev/compare-ui.md` 화면 D 절, `docs/dev/e2e.md`에 compare 스펙·환경변수 추가.

## Files you own
```
apps/web/src/features/compare/ExportScreen.tsx
apps/web/src/features/compare/ExportScreen.test.tsx
apps/web/src/features/compare/exportApi.ts
apps/web/src/state/export.ts
apps/web/src/state/export.test.ts
apps/web/src/i18n/ko.json                    # compare.export.* 추가만
apps/web/src/test-hooks.ts                   # compareRunExport/GetLastRun 등록만
tests/e2e/compare.spec.ts
docs/dev/compare-ui.md                       # 화면 D 절
docs/dev/e2e.md
```

## Forbidden
다른 화면·스토어(패치 제안), `engine/**`, `packages/**`, `apps/desktop/**`, 새 의존성.

## Constraints
- e2e 입력 폴더는 스펙이 임시 폴더로 **복사**해서 쓴다(`fixtures/compare`에 `.halo`나 `출력`이 생기면 안 된다). 테스트 끝에 임시 폴더 삭제.
- 출력 결과 표시는 `Run` 스키마 필드만. 렌더러가 파일을 읽지 않는다(경로만 표시).
- 잡 폴링 정리, 실패 시 오류 문구와 재시도 버튼.

## Definition of done
- vitest `state/export.test.ts`·`ExportScreen.test.tsx`, e2e `compare.spec.ts` 통과, 기존 e2e 유지.
- `tools/verify.sh --e2e` RC=0 (Seed AC4 = `VERIFY: PASS`).
- 커밋 `R1-10: ...` on `task/R1-10`.

## Acceptance checks (Fable이 실행)
```bash
test -f tests/e2e/compare.spec.ts && ls apps/web/src/features/compare | grep -q ExportScreen && echo OK
tools/verify.sh --e2e; echo RC=$?   # RC=0, 출력에 VERIFY: PASS
```

## Defaults for ambiguity
- 실행 날짜를 화면 D에서 바꾸면 export 본문의 `run_date`만 바뀐다(비교 DXF는 그대로).
- 출력 실패 파일이 있어도 나머지는 표시하고 실패 행에 경고.
- TSV 복사 성공 토스트 2초.

## Reference implementation
`features/compare/SetScreen.tsx`(잡 진행 UI), `tests/e2e/compare-review.spec.ts`(훅 사용), `packages/testing/src/fixtures.ts`(스크린샷 첨부).

## Report format
Summary / Files changed / Verification(명령 + 요약 출력, `RC=`, 스크린샷 경로) / Decisions / Deviations from brief / Shared-file patch / Questions for gate / Follow-ups
