# W3-08 P0 후속 정리

- Owner model: Sonnet
- Size: 1.5 d
- Branch: `task/W3-08`
- Wave: 3a  Phase: P1

## Context
G0 보고서(`docs/gates/G0.md` "P1 착수 시 반드시 반영할 후속")의 소규모 항목을 모아 처리한다. 먼저 읽을 것: `CLAUDE.md`, `docs/gates/G0.md`, `docs/contracts/stats-definition.md`, `packages/acad-bridge/README.md`(Known gaps), `engine/src/halo_engine/ingest/stats.py`, `engine/src/halo_engine/validate/crosscheck_report.schema.json`, `packages/schema/README.md`, `apps/desktop/src/main/engine/logger.ts`, `vitest.workspace.ts`.

## Goal
1. 엔진 `ingest/stats.py`·`dxf_loader.py`가 죽은 ATTRIB(`is_alive == False`)과 길이 0 OCS 벡터에서 예외 대신 진단(`diagnostics[]`)을 내고 계속 진행한다(acad-ts 산출 DXF F06/F03으로 회귀 테스트).
2. `packages/schema`에 `CrosscheckReport` 스키마를 편입(`src/stats/crosscheck-report.schema.json`, 엔진 pydantic 모델과 동일성 테스트)하고 TS 타입 생성.
3. `packages/acad-bridge` DXF 라이터 산출물 수복 후처리: ATTRIB 서브클래스 마커 `AcDbAttribute`·태그 그룹 2 복원, MTEXT x축 벡터 정규화, 중복 핸들 재부여 → ezdxf가 읽고 F06 통계가 truth와 일치(`insert X-TITLE` 손실은 알려진 격차로 문서만).
4. `vitest.workspace.ts`에 `packages/schema`, `packages/acad-bridge`, `packages/cad-core`, `packages/dwg-io-gpl` 포함.
5. `electron-log`(MIT) 도입: `apps/desktop/src/main/engine/logger.ts` 내부만 교체, 인터페이스 유지.
6. `.gitignore`·`docs/dev/*` 정리, `docs/dev/setup.md`에 캐피네이트 안내(장시간 작업 시 잠자기 방지).

## Files you own
```
engine/src/halo_engine/ingest/{stats,dxf_loader}.py, engine/tests/ingest/**
packages/schema/src/stats/crosscheck-report.schema.json, packages/schema/{gen,test,examples}/**  (코드젠 재실행)
engine/src/halo_engine/validate/crosscheck_report.schema.json (schema 패키지 사본과 동일성 테스트)
packages/acad-bridge/src/acad/{write,repair-dxf}.ts, packages/acad-bridge/src/**/*.test.ts, README.md
vitest.workspace.ts
apps/desktop/src/main/engine/logger.ts, apps/desktop/package.json (electron-log 의존성)
docs/dev/setup.md
pnpm-lock.yaml, uv.lock
```

## Forbidden
그 외 전부(특히 W3-01~03 소유 영역).

## Constraints
- 각 항목을 별도 커밋으로. 항목마다 테스트.
- 스키마 변경 후 `pnpm --filter @halo-cad/schema build` 두 번 실행 시 diff 0, Python 코드젠 재실행.
- acad-bridge 수복은 라이터 출력 텍스트 후처리로 구현(acad-ts 포크 금지).

## Definition of done
- `tools/verify.sh` 녹색, `tools/crosscheck.sh` 여전히 RED 0.
- 커밋 `W3-08: ...` ×N on `task/W3-08`.

## Acceptance checks
```bash
cd engine && uv run halo-engine stats /tmp/acad-written-F06.dxf --out /tmp/s.json   # no exception, diagnostics listed
node packages/acad-bridge/bin/acad-bridge.mjs dwg2dxf fixtures/generated/F06.dwg /tmp/F06.acad.dxf && cd engine && uv run halo-engine stats /tmp/F06.acad.dxf --out /tmp/s2.json   # readable
pnpm vitest run   # root workspace includes all packages
```

## Defaults for ambiguity
- 진단 형식: `{code, message, handle?, layer?}` 리스트, `LayerStatsDocument`에는 넣지 않고 `*.working.json` 메타에 기록.

## Reference implementation
`packages/acad-bridge/src/acad/decode-fix.ts`(후처리 패턴), `engine/tests/validate/test_report_schema.py`(스키마 동일성 테스트 패턴).

## Report format
Summary / Files changed / Verification / Decisions / Deviations / Shared-file patch / Questions for gate / Follow-ups
