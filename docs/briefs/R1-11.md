# R1-11 최종 설치본과 게이트 문서

- Owner model: Sonnet
- Size: 0.5 d
- Branch: `task/R1-11` (worktree `/Users/ythong/Desktop/대명건설/.worktrees/R1-11`)
- Wave: R1  Phase: D5 (R1-10 병합 후 시작)

## Context
R1의 마지막 합격 판정은 사용자가 Windows 노트북에서 CI 설치본으로 한다: 실도면 68장 도곽 수 상수 일치, 사용자 리비전 쌍의 심은 변경 100% 검출·오탐 0, 마크업 DWG가 ZWCAD 2026에서 열림, 설치본 실행. 이 태스크는 그 확인을 위한 문서·절차·자동 검사 결과를 `docs/gates/R1.md`에 채우고, 남은 문제 목록을 모은다. 코드 변경은 최소(설치본 확인에 필요한 버그 수정만, 패치 제안 우선). 먼저 읽을 것: `CLAUDE.md`, `docs/seeds/R1-mvp-2026-09-04.yaml`(acceptance_criteria·exit_conditions), `docs/gates/R1.md`·`R1-questions.md`, `docs/contracts/r1.md` §12·§13, `docs/dev/{packaging,ci,zwcad-bridge,compare-ingest,compare-frames,compare-engine,compare-export,compare-ui}.md`, 모든 R1 태스크 보고서 요약(Fable이 `docs/gates/R1.md` "남은 문제"에 옮겨 둔 것), `.github/workflows/windows-installer.yml`.

## Goal
1. `docs/gates/R1.md` 완성: 자동 검사 AC1~AC4를 main에서 실제로 실행해 결과(명령·요약·RC)를 채운다. 사용자 확인 절차를 단계별(설치 → 실행 → 세트 지정(실도면 폴더 6개 각각: 전=후 같은 폴더) → 화면 B 도곽 수 읽기 → 리비전 쌍 → 비교 → 승인 → 출력 → ZWCAD로 열기)로 적고, 각 단계에서 **무엇을 어디에 적을지**(도곽 수 표, 클러스터 표, 파일 경로, 스크린샷 파일 이름)를 표로 만든다. `R1-GATE` 네 줄은 사용자가 확인하기 전까지 `(대기)`로 둔다(PASS를 미리 적지 않는다).
2. `docs/gates/R1-user-checklist.md`: 사용자가 인쇄해서 따라갈 수 있는 체크리스트(설치본 링크 자리, ZWCAD 버전 확인, 실도면 경로 기입란, 결과 기입란, 문제 발생 시 로그 위치와 보고 방법).
3. `samples/revision-pairs/README.md`(gitignore 안이지만 README는 추적): 사용자 리비전 쌍 폴더 규약(`<이름>/{before,after}/`, `planted.md` 형식: 도면번호·위치·심은 변경 종류·기대 클러스터 수).
4. Windows 설치본 재확인: CI가 아직 못 돌면(GitHub 저장소 미생성, Q1) `docs/gates/R1.md`에 "설치본: GitHub 저장소 생성 후 첫 푸시에서 생성됨"과 트리거 절차(`git remote add origin … && git push -u origin main && git push origin task/R1-11`)를 적는다. 돌 수 있으면 아티팩트 URL을 적는다.
5. 남은 문제 목록: 각 태스크 보고서의 Follow-ups·Questions와 `R1-questions.md`를 합쳐 우선순위(게이트 차단 / 2주차 / 개선)로 정리.
6. 실도면 도곽 수 mac 측정값(R1-04 보고서 표)을 게이트 문서에 "참고(acad-ts 변환 기준)"로 옮기고 상수와의 차이를 표시한다.

## Files you own
```
docs/gates/R1.md
docs/gates/R1-user-checklist.md
docs/gates/R1-questions.md         # 답변 대기 항목 추가만
samples/revision-pairs/README.md
README.md                          # "사용자 확인" 절 링크 추가만
```

## Forbidden
코드 파일 전부(버그를 찾으면 "Shared-file patch"로 제안). `docs/seeds/**`, `docs/plans/**`.

## Constraints
- 자동 검사는 main 체크아웃이 아니라 **worktree**에서 실행하되 main과 동일 커밋임을 `git log -1`로 적는다.
- 사용자 확인 절차의 모든 문구는 앱의 ko.json 버튼 이름과 일치해야 한다(`apps/web/src/i18n/ko.json`에서 확인).
- `R1-GATE` 줄 형식은 `R1-GATE <항목>: PASS|FAIL` 정확히.

## Definition of done
- `docs/gates/R1.md`에 AC1~AC4 결과·명령·RC, 사용자 절차, 기입란, 남은 문제, `R1-GATE` 4줄(`(대기)`), 사용자 승인란.
- `tools/verify.sh` RC=0(문서만 바뀌므로 녹색이어야 한다).
- 커밋 `R1-11: ...` on `task/R1-11`.

## Acceptance checks (Fable이 실행)
```bash
grep -c "^R1-GATE " docs/gates/R1.md            # 4
test -f docs/gates/R1-user-checklist.md && test -f samples/revision-pairs/README.md && echo OK
```

## Defaults for ambiguity
- 스크린샷 파일 이름 규약: `docs/gates/img/R1-<항목>-<번호>.png`(사용자가 넣는다).
- 리비전 쌍이 아직 없으면 `owner-pair` 절차에 "쌍 제공 후 수행"을 적고 대기로 둔다.

## Report format
Summary / Files changed / Verification(명령 + 요약 출력, `RC=`) / Decisions / Deviations from brief / Shared-file patch / Questions for gate / Follow-ups
