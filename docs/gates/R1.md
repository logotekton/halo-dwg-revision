# R1 게이트 (1주차 MVP)

Seed: `docs/seeds/R1-mvp-2026-09-04.yaml`. 장부: `docs/plans/dms-local/interview-mvp-2026-09-04.md`. 계약: `docs/contracts/r1.md`.

합격 판정 줄은 `R1-GATE <항목>: PASS|FAIL` 형식으로만 적는다. 네 줄이 모두 PASS이고 "사용자 승인" 항목이 채워져야 R1이 끝난다.

## 자동 검사 (Fable, main에서)

| 항목 | 명령 | 결과 |
|---|---|---|
| AC1 선행 병합 | `test -f packages/cad-core/src/host/cad-host.ts && grep -rqi visib packages/cad-core/src/host && test -f engine/src/halo_engine/ingest/xref.py` | OK (2026-09-04, main 4c0cca4: W3-06 병합 3f8cc47, W3-02+레이어 가시성 병합 4c0cca4) |
| AC2 Windows 설치본 CI 정의 | `grep -q windows-latest .github/workflows/windows-installer.yml && grep -qi nsis apps/desktop/electron-builder.yml && grep -q comtypes engine/pyproject.toml` | OK (2026-09-04, main 3b79728; 첫 실제 실행은 저장소 공개 전환 뒤 main@4c0cca4 — 결과는 아래 "설치본" 항목) |
| AC3 엔진 비교 테스트 | `cd engine && uv run --frozen pytest tests/compare -q` | (대기) |
| AC4 verify + e2e | `tools/verify.sh --e2e` | (대기) |

## 사용자 확인 (Windows 노트북, 설치본으로만)

설치본(첫 빌드, main@40375d8, 2026-09-04): https://github.com/logotekton/halo-dwg-revision/actions/runs/33849250597 → Artifacts `halo-dwg-revision-windows-x64`(177 MB zip, 2026-09-18 만료). 잡 4분 20초. D0·D1 범위(셸·엔진 인입 API).
설치본(D2 포함, main@6e4625d, 2026-09-04): https://github.com/logotekton/halo-dwg-revision/actions/runs/33856626045 — 화면 A(세트 지정)·B(도곽 목록), 인입·도곽 추출·매칭 API 포함. CI 필수 잡 전부 녹색. 이 빌드로 `installer`·`sheet-count` 항목을 미리 확인할 수 있다(화면 C·D는 아직 자리표시자). 최종 게이트용 설치본 링크는 R1-11이 갱신한다.

사용자 PC 경로:
- 실도면 68장: (기록 필요)
- 리비전 쌍: (기록 필요)
- ZWCAD: 2026 Professional, (설치 경로 기록)

| 항목 | 절차 | 기대값 |
|---|---|---|
| sheet-count | 화면 A에서 실도면 세트(전=후 동일 폴더도 가능)를 지정 → 화면 B 도곽 수 | 전기 104, 기계 51, 통신 28, 소방기계 45, 소방전기 28, 건축 99 |
| owner-pair | 리비전 쌍 지정 → 비교 → 화면 C 클러스터 | 심은 변경 100% 클러스터, 심지 않은 곳 클러스터 0(접힌 항목 제외) |
| markup-dwg-opens | 화면 D 전체 도곽 출력 → `출력/<날짜>/` DWG를 ZWCAD 2026에서 열기 | 열리고 `REV-<날짜>` 레이어에 클라우드·삼각 번호·리비전 표 |
| installer | 설치본 설치·실행·엔진 준비 상태 | 상태바 "준비", 오류 없음 |

R1-GATE sheet-count: (대기)
R1-GATE owner-pair: (대기)
R1-GATE markup-dwg-opens: (대기)
R1-GATE installer: (대기)

## 남은 문제

태스크 보고서의 Follow-ups를 모은다. 우선순위: **게이트 차단** / 2주차 / 개선. (R1-11이 최종 정리)

| 우선 | 출처 | 내용 |
|---|---|---|
| 게이트 차단 후보 | R1-04 | 실도면 3장(`03_전기` 도서, `05_소방_기계` 내진도면, `01_건축 A-060`)이 mac의 acad-ts 변환 경로에서 MULTILEADER `90 / None` 결함으로 못 읽힘. Windows(ZWCAD 변환)에서는 안 날 것으로 예상 — 게이트에서 이 3장의 도곽 수를 확인(Q9) |
| 게이트 확인 | R1-04 | 기본 `frames.yaml`(ATTRIB 태그)로 실도면 도곽 수가 나오는지 Windows에서 확인. mac에서는 acad-ts가 ATTRIB을 잃어 `block_name_patterns: ["*TITLE BLOCK*"]` 경로로만 측정(기계 51·통신 28·소방전기 28 일치) |
| 게이트 확인 | R1-02/R1-03 | 실제 ZWCAD 자동화(숨은 인스턴스, DWG→DXF, 시간 초과·재시작, 같은-변환기 재변환)는 가짜 COM으로만 검증됨(Q6) |
| 게이트 확인 | R1-00a | Windows 설치본에서 뷰어 WebGL 렌더와 숨김 창 DWG 변환(폴백 경로) 확인 |
| 2주차 | R1-03 | `ingest/working_dxf.py`가 캐시 키를 입력 sha로 잡아 DWG 입력 시 원본 sha 이름으로 정본을 한 벌 더 복사(디스크 2배). `cache_key` 인자 추가 제안 |
| 2주차 | R1-03/R1-04/R1-05 | `halo_schema`(packages/schema/gen/python)를 엔진 의존성으로, `@halo-cad/schema`를 apps/web 의존성으로 배선하면 손수 미러한 응답 모델·TS 타입을 걷어낼 수 있음 |
| 2주차 | R1-04 | `sheet-frame` 스키마에 `file_name`(선택) 추가, `repos.create_pair` 추가, `boundary_source` 저장 |
| 2주차 | R1-04 | `03_전기`(20.8 MB, 348k 엔티티)의 acad-ts 변환이 병렬 실행 중 실패(단독 343초 성공) |
| 2주차 | R1-05 | WS `job.progress` 클라이언트(지금은 500ms 폴링, 엔진 원문 message 표시). DXF 입력일 때 요약 카드 "변환 성공 0" 문구 다듬기 |
| 2주차 | R1-00a | 뷰어 힙 20회 왕복 +8.6%, mlightcad 단일 청크 3.56 MB, `apps/web`이 `@halo-cad/dwg-io-gpl`에 의존(convert.html 청크에만), 폰트 없어 TEXT가 비어 보임(W3-05 범위) |
| 개선 | R1-05 | 옛 셸 e2e `tests/e2e/shell.spec.ts`를 `test.skip`으로 은퇴(승인). 삭제 여부는 2주차 정리 때 |
| 개선 | Fable | `test_engine_crosscheck`는 Windows에서 skip(설치 폰트에 따른 TEXT 범위 차이). Windows verify 레그는 비필수 유지 |

## 사용자 승인

- 날짜: (대기)
- 승인 문구: (대기)
