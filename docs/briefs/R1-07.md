# R1-07 합성 리비전 쌍 생성기와 truth

- Owner model: Sonnet
- Size: 1 d
- Branch: `task/R1-07` (worktree `/Users/ythong/Desktop/대명건설/.worktrees/R1-07`)
- Wave: R1  Phase: D2 (R1-01 병합 후 시작; R1-04·R1-06·R1-10 테스트가 이 산출물을 쓴다)

## Context
비교 엔진(R1-06)의 합격 기준은 "심은 변경 100% 검출, 접힘 규칙 정확, 통째 사본에서 오탐 0"이다. 그 정답을 내는 결정론적 생성기가 필요하다. 기존 `fixtures/gen/`(독립 uv 프로젝트, ezdxf만 의존, 결정론 테스트)과 같은 방식으로 `fixtures/compare/gen/`을 만든다. 먼저 읽을 것: `CLAUDE.md`, `docs/contracts/r1.md` §4(`RevisionTruth`)·§5(`frames.yaml` 표제란 규칙 — 생성기가 만드는 표제란이 이 규칙에 걸려야 한다)·§12, `docs/contracts/compare-dxf.md` §5(클라우드 크기 기준 축척), `packages/schema/src/compare/truth.schema.json`(R1-01), `fixtures/README.md`, `fixtures/gen/src/fixtures_gen/{base,common,pipeline,cli}.py`, `fixtures/gen/src/fixtures_gen/fixtures/f06_structural_plan.py`(도곽·표제란 예), `fixtures/gen/tests/test_determinism.py`.

## Goal
1. `fixtures/compare/gen/`(uv 프로젝트 `compare_fixtures_gen`, 의존성 `ezdxf==1.4.4`, dev `pytest`, `jsonschema`)이 `fixtures/compare/<scenario>/before/*.dxf`, `after/*.dxf`, `truth.json`을 만든다. 명령: `cd fixtures/compare/gen && uv run python -m compare_fixtures_gen --out .. [--only S03]`.
2. 기준 도면 `base_plan`: R2018 DXF, 모델공간, mm. 표제란 블록 `TITLEBLOCK`(ATTRIB `DWG_NO`, `TITLE`, `SCALE`="1:100", `DATE`, `PROJECT` — 5개), 도곽 사각형 LWPOLYLINE(A1 841×594 × 100 = 84,100×59,400), 표제란은 오른쪽 아래. 내용: 그리드 X1–X5/Y1–Y4(LINE + 원 버블 INSERT `GRID_BUBBLE` with ATTRIB), 이중선 벽 LWPOLYLINE, 문 INSERT `DOOR_900`(ATTRIB TAG), 창 INSERT `WIN_1800`, 기둥 12개(LWPOLYLINE + SOLID 해치), 실명 TEXT 6, 범례 MTEXT(서식 코드 `\P`, `{\fArial;}`), 치수 DIMENSION 4(선형), 해치 ANSI31 2, 주석 LEADER 1. 레이어 8개 이상(`A-WALL`, `A-DOOR`, `A-WIND`, `A-COL`, `A-GRID`, `A-TEXT`, `A-DIM`, `A-HATCH`, `TITLE`).
3. 시나리오(각각 `truth.json`; 전·후 도곽 좌표는 같다):
   | ID | 심는 변경 | 기대 |
   |---|---|---|
   | `S01_identical` | 없음 | pair `same`, cluster 0 |
   | `S02_move_door` | 문 INSERT 1개를 동쪽 1,250mm 이동 | `moved` 1, cluster 1 |
   | `S03_dim_value` | 치수 1개의 측정점을 옮겨 값 변경 | `dimension` 1 |
   | `S04_text_change` | 실명 TEXT 1개 문구 변경 | `text` 1 |
   | `S05_added` | 벽 폴리라인 1 + 문 INSERT 1 신설(서로 3m 이상 떨어짐) | `added` 2, cluster 2 |
   | `S06_removed` | 기둥 1개(폴리라인+해치) 삭제 | `removed` 2, cluster 1 |
   | `S07_hatch_regen` | 해치 1개를 같은 경계로 다시 생성(핸들 바뀜, 시드 포인트 변경) | `minor`, reason `hatch_regen`, cluster 0 |
   | `S08_layer_only` | 벽 3개 레이어를 `A-WALL2`로 | `minor` 3, reason `layer_only`, cluster 0 |
   | `S09_mtext_format_only` | MTEXT 서식 코드만 변경(글자 동일) | `minor` 1, `mtext_format_only` |
   | `S10_move_tiny` | 문 INSERT 1개 0.005mm 이동 | `minor` 1, `move_le_0_01` |
   | `S11_blockdef_change` | `DOOR_900` 블록 정의 내부 선 하나 길이 변경(인스턴스 6) | `blockdef` 1(인스턴스 수 6) |
   | `S12_whole_redraw` | 후 도면을 **다른 순서로 다시 생성**해 핸들 전부 변경 + S02·S04를 심음 | `moved` 1, `text` 1, 나머지 오탐 0 |
   | `S13_multi_sheet` | 한 파일에 도곽 2개(`A-101`, `A-102`, 가로 배치), 변경은 `A-102`에만 S05 | `A-101` same, `A-102` changed |
   | `S14_sheet_added_removed` | 전에는 파일 `A-101`·`A-102`, 후에는 `A-102`·`A-103` | `A-101` removed, `A-103` added |
   | `S15_frame_shift` | 후 파일 전체를 (+50,000, +20,000) 평행이동 | `same`(도곽 로컬 좌표) |
   | `S16_unrecognized` | 후 세트에 표제란 없는 파일 `detail.dxf` 추가 | 그 파일 `unrecognized` |
   | `S17_scale_50` | `SCALE`="1:50" 도곽(A1 × 50)에 S02 | `moved` 1, `scale_denominator` 50 |
4. `truth.json`은 `compare/truth.schema.json`을 통과해야 한다(`jsonschema`로 테스트). `expected_changes[]`는 kind·etype·layer·handle(알 수 있으면)·bbox(반드시)·minor·minor_reason·note. `expected_cluster_count`, `clean_regions`(변경이 없는 큰 영역 2곳 이상).
5. 결정론: 같은 명령 두 번 → `fixtures/compare/**` 바이트 동일(테스트 + `git status`). 핸들 안정: 전·후는 같은 생성 코드가 같은 순서로 만들고 심는 변경은 생성 뒤 **제자리 수정**(S12만 예외: 생성 순서를 결정론적으로 뒤집는다 — 난수 금지).

## Inputs
- `fixtures/gen`의 `base.py`·`common.py`(표제란·ATTRIB 만드는 헬퍼)를 복사해 써도 된다(임포트 의존은 만들지 않는다).

## Files you own
```
fixtures/compare/**
fixtures/README.md          # compare 절 추가
.gitignore                  # 필요 시 fixtures/compare/gen/.venv 등
```

## Forbidden
`fixtures/gen/**`, `fixtures/generated/**`, `fixtures/truth/**`, `engine/**`, `packages/**`, `apps/**`.

## Constraints
- ezdxf 1.4.4만. DXF는 `AC1032`, `$INSUNITS=4`. 헤더 `$FINGERPRINTGUID`·`$VERSIONGUID`·`$TDCREATE` 등은 상수로 고정(결정론).
- 파일 크기: 시나리오당 200KB 이하.
- 도곽 안 엔티티만 심는다(도곽 밖은 R1에서 비교 제외). 표제란 ATTRIB 태그는 `frames.yaml` 기본 목록에 들어 있는 이름을 쓴다.
- `truth.json`의 bbox는 후 도면 세계 좌표(`moved`는 전·후 합집합). 소수점 3자리.
- 생성기는 `fixtures/compare/gen/src/compare_fixtures_gen/{__main__,cli,base_plan,plant,scenarios/*.py,truth.py}` 구조. 시나리오마다 파일 하나.

## Definition of done
- 테스트(`fixtures/compare/gen/tests/`): `test_determinism.py`(두 번 생성 바이트 동일), `test_truth_schema.py`(17개 truth가 스키마 통과), `test_frames_recognizable.py`(ezdxf로 다시 읽어 표제란 INSERT 수 = 기대 도곽 수, ATTRIB 태그 존재), `test_planted_bboxes_inside_frame.py`.
- 생성 산출물(`fixtures/compare/S01_*/…`)을 **커밋**한다(작아서 가능).
- `tools/verify.sh` RC=0(이 태스크는 TS·엔진에 손대지 않으므로 기본적으로 녹색이어야 한다).
- 문서 `fixtures/README.md` "합성 리비전 쌍" 절: 재생성 명령, 시나리오 표, truth 형식 요약.
- 커밋 `R1-07: ...` on `task/R1-07`.

## Acceptance checks (Fable이 실행)
```bash
ls -d fixtures/compare/S* | wc -l                                       # 17
cd fixtures/compare/gen && uv sync && uv run pytest -q                  # passed
cd fixtures/compare/gen && uv run python -m compare_fixtures_gen --out .. && git -C ../../.. status --short fixtures/compare | wc -l   # 0
```

## Defaults for ambiguity
- 치수 값 변경(S03)은 정의점 이동으로 만들고 `text override`는 쓰지 않는다.
- 해치 재생성(S07)은 같은 경계 정점을 다른 시작점부터 순서만 바꿔 넣는다(경계 bbox 동일).
- S12는 엔티티 생성 순서를 시나리오 목록의 역순 + 레이어 생성 순서 역순으로 한다.
- 표제란 도면번호는 `A-101` 형식, 제목은 한글(`1층 평면도`).

## Reference implementation
`fixtures/gen/src/fixtures_gen/fixtures/f06_structural_plan.py`, `fixtures/gen/src/fixtures_gen/pipeline.py`(출력·결정론), `fixtures/gen/tests/test_determinism.py`.

## Report format
Summary / Files changed / Verification(명령 + 요약 출력, `RC=`) / Decisions / Deviations from brief / Shared-file patch / Questions for gate / Follow-ups
