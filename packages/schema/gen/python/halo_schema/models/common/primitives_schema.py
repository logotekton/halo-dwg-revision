# generated by datamodel-codegen:
#   filename:  common/primitives.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import BaseModel, ConfigDict, Field, RootModel


class Primitives(RootModel[Any]):
    root: Any = Field(..., title='Primitives')
    """
    Shared scalar and geometry definitions reused by every Halo CAD contract schema. All lengths are millimetres (float64) as required by docs/PLAN.md 5.
    """


class Ulid(RootModel[str]):
    root: str = Field(..., pattern='^[0-7][0-9A-HJKMNP-TV-Z]{25}$', title='Ulid')
    """
    Crockford base32 ULID (26 characters, uppercase). Used for every identifier Halo CAD generates.
    """


class Sha256(RootModel[str]):
    root: str = Field(..., pattern='^[0-9a-f]{64}$', title='Sha256')
    """
    Lowercase hex sha256 digest of a file's bytes.
    """


class Handle(RootModel[str]):
    root: str = Field(..., pattern='^[0-9A-F]{1,16}$', title='DxfHandle')
    """
    DXF entity handle as written in the working DXF: uppercase hexadecimal, no leading `0x`, 1-16 digits. `(file, handle)` is the cross-parser key defined by ADR-0002.
    """


class Confidence(RootModel[float]):
    root: float = Field(..., ge=0.0, le=1.0, title='Confidence')
    """
    Confidence in [0, 1]. 1.0 means read directly and unambiguously from the drawing.
    """


class Point(RootModel[list[float]]):
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """
    root: list[float] = Field(..., max_length=3, min_length=2, title='Point')
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """


class Bbox(BaseModel):
    """
    Axis-aligned bounding box in millimetres. `min` and `max` must have the same arity.
    """
    model_config = ConfigDict(
        extra='forbid',
    )
    min: list[float] = Field(..., max_length=3, min_length=2, title='Point')
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """
    max: list[float] = Field(..., max_length=3, min_length=2, title='Point')
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """


class Transform(BaseModel):
    """
    Block reference placement. `matrix` is the optional row-major 4x4 equivalent; when both are present they must agree.
    """
    model_config = ConfigDict(
        extra='forbid',
    )
    insert: list[float] = Field(..., max_length=3, min_length=2, title='Point')
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """
    scale: list[float] | None = Field([1, 1, 1], max_length=3, min_length=3)
    rotation_deg: float | None = 0
    normal: list[float] | None = Field(None, max_length=3, min_length=2, title='Point')
    """
    `[x, y]` or `[x, y, z]` in millimetres, working-DXF world coordinates.
    """
    matrix: list[float] | None = Field(None, max_length=16, min_length=16)


class LevelKind(StrEnum):
    """
    The four height fields kept separate by ADR-0003. `SL` structural level, `FL` finished floor level, `FLOOR_HEIGHT` storey height (SL to SL), `CH` ceiling height (level/finish table). Never equality-compare `CH` against the other three.
    """
    SL = 'SL'
    FL = 'FL'
    FLOOR_HEIGHT = 'FLOOR_HEIGHT'
    CH = 'CH'


class LevelSource(StrEnum):
    """
    Which drawing or table a height value was read from. `LEVEL_TABLE` and `FINISH_SCHEDULE` are the sources that yield `CH`; `ELEVATION`, `SECTION` and `STRUCT_PLAN` yield `SL` and `FL` (ADR-0003). `CEILING_PLAN` also yields `CH`, only when the ceiling plan drawing itself labels a ceiling height (ADR-0003 addendum, 2026-09-03) -- it is the one source a `CH`<->`CH` consistency check may compare against `LEVEL_TABLE`/`FINISH_SCHEDULE` with `EQ` (`levels/consistency-check.schema.json`).
    """
    ELEVATION = 'ELEVATION'
    SECTION = 'SECTION'
    STRUCT_PLAN = 'STRUCT_PLAN'
    LEVEL_TABLE = 'LEVEL_TABLE'
    FINISH_SCHEDULE = 'FINISH_SCHEDULE'
    CEILING_PLAN = 'CEILING_PLAN'
    USER = 'USER'


class ClassificationSource(StrEnum):
    """
    How a classification was produced (docs/PLAN.md 4).
    """
    AUTO_RULE = 'AUTO_RULE'
    AUTO_ML = 'AUTO_ML'
    USER = 'USER'
