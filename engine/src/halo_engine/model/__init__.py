"""Part/space/surface model (member DB, solids, prisms, surface attributes).

Empty in W1-02; strict mypy is already configured for this package (see
``engine/pyproject.toml``) so the first real module here is type-checked
from day one.
"""
